buildscript {
    ext {
        buildToolsVersion = "35.0.0"
        minSdkVersion = 24
        compileSdkVersion = 35
        targetSdkVersion = 35
        // NDK r28+: 16 KB ELF alignment by default (arm64-v8a, x86_64). Required for Play 16 KB page-size devices.
        // Install via SDK Manager → NDK if Gradle reports this revision is missing.
        ndkVersion = "28.2.13676358"
        kotlinVersion = "2.0.21"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"

// Transitive deps may pull androidx.core 1.17+ (needs compileSdk 36 + AGP 8.9+). RN 0.77 pins AGP 8.7 / compileSdk 35.
subprojects { subproject ->
    subproject.configurations.configureEach { configuration ->
        configuration.resolutionStrategy {
            force "androidx.core:core:1.15.0"
            force "androidx.core:core-ktx:1.15.0"
        }
    }
}
